---
const { title, description, ctaText, ctaLink } = Astro.props;
---

<section class="py-12 px-6 text-center bg-yellow-50 text-black rounded-xl shadow-lg">
  <h1 class="text-4xl font-bold mb-4">{title}</h1>
  <p class="text-lg mb-6">{description}</p>
  <a href={ctaLink} class="inline-block bg-black text-yellow-400 px-6 py-3 rounded-lg font-semibold hover:bg-gray-900 transition">
    {ctaText}
  </a>
</section>
